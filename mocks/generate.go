package mocks

//go:generate mockgen -source=../repository/interface.go -destination=repository.go -package=mocks
//go:generate mockgen -source=../usecase/interface.go -destination=usecase.go -package=mocks
